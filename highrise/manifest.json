{
  "name": "Highrise",
  "private": true,

  "configuration": {
    "subdomain": {
      "type": "url",
      "required": "true"
    },

    "token": {
      "type": "text",
      "required": "true"
    },

    "useSSL": {
      "type": "boolean",
      "required": "true"
    }
  }
}
